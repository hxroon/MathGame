# MathGame
A three level based math game where user can decide difficulty level


"So the idea is to create an agent that can take an Excel workbook and produce a data model or process map showing how information flows through the file, what the inputs and outputs are, and what transformations happen between tabs. That could then support SOP creation and help with Excel-to-Planet migration work."
